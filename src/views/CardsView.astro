---
interface Props {
    title: string;
    bgView?: string;
}

const { title, bgView } = Astro.props;
---

<section class={`${bgView} px-6 py-12`}>
    <h3 class="text-white text-2xl text-center font-semibold">
        {title}
    </h3>

    <div class="flex flex-col md:flex-row items-center mt-8 gap-6">
        <slot/>
    </div>
</section>